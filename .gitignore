# CI / build scratch — not committed.
# (gitignore has no inline comments — keep patterns on their own lines)
#   _packages/ : sibling package repos checked out during the Pages workflow
#   _site/     : the assembled deploy artifact
/_packages/
/_site/

# macOS
.DS_Store
